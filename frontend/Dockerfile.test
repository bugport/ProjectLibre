# Run Vitest in a container
FROM node:22-alpine
WORKDIR /app

# Install dependencies (use lockfile if present)
COPY package.json package-lock.json* ./
RUN npm ci || npm install

# Copy source
COPY . .

# Run tests (can be overridden)
CMD ["npm", "run", "test"]
