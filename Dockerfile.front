# Runtime for serving built frontend (static assets)
# Expects ./frontend/dist to be present (built via Gradle task or locally)
FROM nginx:alpine

# Copy built assets
COPY ./frontend/dist/ /usr/share/nginx/html/

# Expose HTTP port
EXPOSE 80

# Start nginx in foreground
CMD ["nginx", "-g", "daemon off;"]
